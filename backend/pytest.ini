[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    unit: small unit tests with deterministic mocks
    integration: tests that use stubbed services
    contract: schema validation tests
    canary: tests that call real LLM providers (non-blocking)
    e2e: full end-to-end user flow tests
