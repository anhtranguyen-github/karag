Metrics conventions:

- Prometheus remains the default metrics sink
- backend request, provider, ingestion, and retrieval metrics should be defined once
- dashboards and alert rules should be added here as the platform matures
